{
  "name": "gst-invoice-generator",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack GST invoice generator for hospitality businesses.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace client",
    "test": "npm run test --workspace server",
    "lint": "npm run lint --workspace client",
    "start": "npm run start --workspace server",
    "db:create-indexes": "npm run db:create-indexes --workspace server",
    "db:migrate-single-company-tenancy": "npm run db:migrate-single-company-tenancy --workspace server",
    "db:encrypt-sensitive-data": "npm run db:encrypt-sensitive-data --workspace server",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,css}\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18.16.0",
    "npm": ">=9.7.0"
  }
}
